seed: 42

models:
  policy:  
    min_log_std: -20.0
    max_log_std: 2.0
  value: 
    clip_actions: False

  model_type: null
  shared: False
  squashed: False
  
buffer:
  buffer_size: -1

agent:
  class: PPO
  rollouts: 32
  learning_epochs: 5
  mini_batches: 4
  discount_factor: 0.99
  lambda: 0.95
  learning_rate: 5.0e-05
  random_timesteps: 0
  learning_starts: 0
  grad_norm_clip: 1.0
  ratio_clip: 0.2
  value_clip: 0.2
  entropy_loss_scale: 0.01
  value_loss_scale: 1.0
  kl_threshold: 0.0
  time_limit_bootstrap: True
  clip_predicted_values: False
  async_actor_critic: False
  
  # Action scaling (Env-specific)
  action_scale_factor:
    joint: null 
    wheel: null

  # Logging and checkpoint
  experiment:
    directory: "GOAT_Stand_DR_PP"
    experiment_name: ""
    write_interval: auto
    checkpoint_interval: auto

train:
  timesteps: 32000
